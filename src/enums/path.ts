export enum PATH {
    HOME = "/",
    PROFILE = "/profile/:id",
    MESSAGES = "/messages",
    USERS = "/users",
    NEWS = "/news",
    MUSIC = "/music",
    SETTINGS = "/settings",
    LOGIN = "/login",
}